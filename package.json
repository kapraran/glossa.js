{
  "name": "glossa.js",
  "version": "0.0.1",
  "description": "A glossa (γλώσσα) interpreter in Typescript",
  "author": "Nikolaos Kapraras",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^5.2.6",
    "@types/node": "^10.12.0",
    "chevrotain": "^6.0.0",
    "mocha": "^5.2.0",
    "prettier": "^1.14.3",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  },
  "scripts": {
    "tsc": "tsc",
    "example": "tsc && node dist/main test/glossa/sample.glossa",
    "prettier": "prettier --write 'src/**/*.{ts, js}'",
    "diagram": "tsc && node dist/tools/gen-diagram",
    "test": "mocha --require ts-node/register test/**/*.test.ts"
  }
}
